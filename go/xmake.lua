-- define target
target("readlines")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("readlines.go")

-- define target
target("tst_time")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_time.go")

-- define target
target("tst_http_multi_dir")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_http_multi_dir.go")

-- define target
target("tst_select")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_select.go")

-- define target
target("defer_tracing")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("defer_tracing.go")

-- define target
target("tst_slice_make_append")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_slice_make_append.go")

-- define target
target("color")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("color.go")

-- define target
target("partition")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("partition.go")

-- define target
target("defer_logvalues")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("defer_logvalues.go")

-- define target
target("fmt")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fmt.go")

-- define target
target("fmt_errorf")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fmt_errorf.go")

-- define target
target("tst_context_multi")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_context_multi.go")

-- define target
target("tst_strconv")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_strconv.go")

-- define target
target("for_arrays")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("for_arrays.go")

-- define target
target("tst_reflect_decodeStruct2Map")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_reflect_decodeStruct2Map.go")

-- define target
target("tst_reflect")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_reflect.go")

-- define target
target("tst_chan")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_chan.go")

-- define target
target("random")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("random.go")

-- define target
target("function_return")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("function_return.go")

-- define target
target("tst_for_select")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_for_select.go")

-- define target
target("function_calls_function")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("function_calls_function.go")

-- define target
target("tst_context")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_context.go")

-- define target
target("tst_add")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_add.go")

-- define target
target("tst_timer01")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_timer01.go")

-- define target
target("tst_string_split")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_string_split.go")

-- define target
target("fileinput")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fileinput.go")

-- define target
target("tst_chan_and_go")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_chan_and_go.go")

-- define target
target("tst-sort_strings")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst-sort_strings.go")

-- define target
target("tst_timer00")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_timer00.go")

-- define target
target("defer_tracing2")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("defer_tracing2.go")

-- define target
target("string_conversion2")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("string_conversion2.go")

-- define target
target("switch1")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("switch1.go")

-- define target
target("tst_reflect_examiner")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_reflect_examiner.go")

-- define target
target("tst_time_utc_local")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_time_utc_local.go")

-- define target
target("tst")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst.go")

-- define target
target("function_literal")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("function_literal.go")

-- define target
target("tst_if")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_if.go")

-- define target
target("tst_panic")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_panic.go")

-- define target
target("tst_rune")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_rune.go")

-- define target
target("defer")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("defer.go")

-- define target
target("count_characters")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("count_characters.go")

-- define target
target("switch2")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("switch2.go")

-- define target
target("tst_logrus")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_logrus.go")

-- define target
target("tst_slice_para")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_slice_para.go")

-- define target
target("tst_multi_line_string")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_multi_line_string.go")

-- define target
target("global_scope")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("global_scope.go")

-- define target
target("tst_http_multi_port")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_http_multi_port.go")

-- define target
target("local_scope")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("local_scope.go")

-- define target
target("parser")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("parser.go")

-- define target
target("examp_json")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("examp_json.go")

-- define target
target("tst_enum")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_enum.go")

-- define target
target("tst_string_find")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_string_find.go")

-- define target
target("parse_json")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("parse_json.go")

-- define target
target("fibonacci")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fibonacci.go")

-- define target
target("for1")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("for1.go")

-- define target
target("tst_waitgroup")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_waitgroup.go")

-- define target
target("tst_nil")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_nil.go")

-- define target
target("fibonacci_closure")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("fibonacci_closure.go")

-- define target
target("filescaner")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("filescaner.go")

-- define target
target("tst_inside")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_inside.go")

-- define target
target("tst_sort_struct")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_sort_struct.go")

-- define target
target("goos")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("goos.go")

-- define target
target("tst_signal")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_signal.go")

-- define target
target("tst_json")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_json.go")

-- define target
target("tst_slice")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_slice.go")

-- define target
target("for_string")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("for_string.go")

-- define target
target("tst_map")

    -- set kind
    set_kind("binary")

    -- add files
    add_files("tst_map.go")

--
-- FAQ
--
-- You can enter the project directory firstly before building project.
--   
--   $ cd projectdir
-- 
-- 1. How to build project?
--   
--   $ xmake
--
-- 2. How to configure project?
--
--   $ xmake f -p [macosx|linux|iphoneos ..] -a [x86_64|i386|arm64 ..] -m [debug|release]
--
-- 3. Where is the build output directory?
--
--   The default output directory is `./build` and you can configure the output directory.
--
--   $ xmake f -o outputdir
--   $ xmake
--
-- 4. How to run and debug target after building project?
--
--   $ xmake run [targetname]
--   $ xmake run -d [targetname]
--
-- 5. How to install target to the system directory or other output directory?
--
--   $ xmake install 
--   $ xmake install -o installdir
--
-- 6. Add some frequently-used compilation flags in xmake.lua
--
-- @code 
--    -- add macro defination
--    add_defines("NDEBUG", "_GNU_SOURCE=1")
--
--    -- set warning all as error
--    set_warnings("all", "error")
--
--    -- set language: c99, c++11
--    set_languages("c99", "cxx11")
--
--    -- set optimization: none, faster, fastest, smallest 
--    set_optimize("fastest")
--    
--    -- add include search directories
--    add_includedirs("/usr/include", "/usr/local/include")
--
--    -- add link libraries and search directories
--    add_links("tbox", "z", "pthread")
--    add_linkdirs("/usr/local/lib", "/usr/lib")
--
--    -- add compilation and link flags
--    add_cxflags("-stdnolib", "-fno-strict-aliasing")
--    add_ldflags("-L/usr/local/lib", "-lpthread", {force = true})
--
-- @endcode
--
-- 7. If you want to known more usage about xmake, please see http://xmake.io/#/home
--
    
