#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

#define MAX_LEN 1024

int main()
{
    // char tmp[] = "This is a test.\n";
    // printf("%d\n", strlen(tmp));	// 没有计入结束符
    // printf("%d\n", sizeof(tmp));	// 计入结束符

    // int a = 0;
    // char b = 0x01;
    // b == 0x01 ? a = 0 : a = 1;
    // printf("%d\n", a);

    return 0;
}
